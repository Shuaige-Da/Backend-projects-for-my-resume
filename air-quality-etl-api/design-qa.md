# 设计验收记录

最终结果：passed

## 验收基准

- 用户确认的方向：浅色主题、天空蓝导航、透明白云质感、白色内容卡片。
- 信息结构：数据分析、数据质量、中文 API 文档三个一级入口。
- 数据约束：数据源没有行政区字段，树状关系使用“全台 → 县市 → 监测站”，不补造区县。
- 语义颜色：AQI 六级状态分别使用绿、黄、橙、红、紫、褐红，卡片和按钮统一使用蓝白体系。

## 桌面端检查（1440 × 1000）

- 导航、筛选区、指标卡、趋势图、排名表和质量卡片层级清晰，无内容遮挡。
- 选择“臺北市”后，标题、核心指标和排名同步切换，排名表展示真实监测站数据。
- 中文 API 文档可展开，参数位置、必填状态、可空字符串类型和响应结构均正常显示。
- 浏览器控制台无 error 或 warning。

## 窄屏检查

- 导航按钮保持可点击，县市树、筛选控件和内容卡片改为纵向排列。
- 表格保留横向滚动，不压缩关键字段。

## 运行证据

- 正式看板截图：`docs/images/dashboard.png`
- 中文 API 文档截图：`docs/images/api-docs.png`
- 前端生产构建：通过。
- 后端单元与契约测试：21 passed，2 skipped。
- PostgreSQL 隔离集成测试：2 passed。
